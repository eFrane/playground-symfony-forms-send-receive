{% extends 'base.html.twig' %}

{% block body %}
    {% if task is defined %}
    <h1>Edit Task {{ task.id }}</h1>
    {% else %}
    <h1>New Task</h1>
    {% endif %}

    <a href="{{ path('task.list') }}">Back to overview</a>
    <br><br>

    {{ form(form) }}
{% endblock body %}
